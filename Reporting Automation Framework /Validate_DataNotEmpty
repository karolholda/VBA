'===========================
' Funkcja: DataNotEmpty
'===========================

' Test sprawdzający, czy dane nie są puste

    Private Function Validate_DataNotEmpty() As Boolean
        Validate_DataNotEmpty = Application.WorksheetFunction.CountA(Sheets(DATA_SHEET).Cells) > 1
    End Function
