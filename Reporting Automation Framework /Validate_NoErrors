'===========================
' Funkcja: Validate_NoErrors
'===========================

' Sprawdza błędy Excela typu: #N/A, #VALUE!, #DIV/0!, #REF!, #NUM!

    Private Function Validate_NoErrors() As Boolean
        Dim ws As Worksheet
        Dim rng As Range
        Dim cell As Range
    
        Set ws = ThisWorkbook.Worksheets(DATA_SHEET)
    
' Określ obszar danych do sprawdzenia
        Set rng = ws.UsedRange
    
' Przejdź przez każdą komórkę w obszarze
        For Each cell In rng.Cells
    
' Sprawdź, czy komórka zawiera błąd Excela
            If IsError(cell.Value) Then
                Validate_NoErrors = False
                Exit Function
            End If
    
        Next cell
    
' Jeśli nie znaleziono żadnych błędów
        Validate_NoErrors = True
    End Function
