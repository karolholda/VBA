Function DataQualityGate() As Boolean

' sprawdzenie czy są kolumny, jesli choć jedna pusta komórka to test jakości niezaliczony

    If WorksheetFunction.CountBlank(Sheets("Data").Range("A:A")) > 0 Then
        DataQualityGate = False
        Exit Function
    End If
    DataQualityGate = True
End Function
